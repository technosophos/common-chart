# Default values for fullname.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
fullnamePrefix: "pre-"
fullnameSuffix: "-suf"
global:
  fullnamePrefix: "pp-"
  fullnameSuffix: "-ps"
